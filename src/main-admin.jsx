import React from 'react';
import ReactDOM from 'react-dom/client';
import AdminPortal from './AdminApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AdminPortal />
  </React.StrictMode>
);